# CMakeLists.txt

IF (INSTALL_FFPRESETS AND FFMPEG_SHAREDIR)
  FILE(GLOB FFPRESET_FILES "${CMAKE_CURRENT_SOURCE_DIR}/*.ffpreset")
  INSTALL (FILES ${FFPRESET_FILES}
    DESTINATION ${FFMPEG_SHAREDIR}
    COMPONENT ffpreset
  )
ENDIF (INSTALL_FFPRESETS AND FFMPEG_SHAREDIR)

## EOF
